<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<!-- better to use link_to    <a href="/users/sign_in">SignIn</a>  | <a href="/users/sign_out" data-method="delete">SignOut</a> -->

  Hello, <% if user_signed_in? %>
   <%= current_user.username.capitalize %>    ||   <%= link_to 'Sign Out!', destroy_user_session_path, method: :delete %>

  <% else %> 
    <%= link_to 'Sign In!', new_user_session_path  %>
  <% end %>

   <p class="notice"><%= notice %></p>
   <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
